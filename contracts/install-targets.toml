[targets.claude]
description = "Ignored, on-demand flat skill surface for Claude-compatible consumers."
dest = ".dist/claude"
include_internal_runtime_support = false

[targets.codex]
description = "Ignored, on-demand flat skill surface for Codex-compatible consumers."
dest = ".dist/codex"
include_internal_runtime_support = false

[targets.root-flat]
description = "Generated repository-root compatibility surface used by local plugin commands and current symlink exposure."
dest = "skills"
include_internal_runtime_support = true

[targets.all]
members = ["claude", "codex", "root-flat"]
