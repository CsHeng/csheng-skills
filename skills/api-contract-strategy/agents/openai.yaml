interface:
  display_name: "API Contract Strategy"
  short_description: "Design lean API contract and verification boundaries"
  default_prompt: "Use $api-contract-strategy to assess API ownership, compatibility, and layered verification."
