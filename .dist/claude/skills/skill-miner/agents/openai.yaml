interface:
  display_name: "Skill Miner"
  short_description: "Mine history, docs, and memory cleanup candidates"
  default_prompt: "Use $skill-miner to mine sessions, project docs, and memory files for skill improvements and durable repo-truth extraction."
policy:
  allow_implicit_invocation: false
